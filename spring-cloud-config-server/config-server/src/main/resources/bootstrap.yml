# 대칭 키 설정
#encrypt:
#  key: s3cr3t

# 비대칭 키 설정
encrypt:
  key-store:
    alias: tacokey
    location: classpath:/keystore.jks
    password: l3tm31n
    secret: s3cr3t

#spring:
#  cloud:
#    config:
#      server:
#        encrypt:
#          enabled: false # 암호화 되어있는 데이터를 복호화하지 않는다.
